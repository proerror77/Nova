# Match Configuration
# For more information, see: https://docs.fastlane.tools/actions/match/

# Git repository URL for storing certificates and profiles
# Private certificates repository
git_url(ENV["MATCH_GIT_URL"] || "https://github.com/proerror77/for_icered.git")

# Storage mode (git, google_cloud, s3, gitlab_secure_files)
storage_mode("git")

# Type of provisioning profile
type("appstore")

# App identifier(s)
app_identifier(["com.app.icered"])

# Your Apple Developer Portal username
username(ENV["APPLE_ID"] || "sonic@icered.com")

# Team ID
team_id(ENV["TEAM_ID"] || "2C77AZCA8W")

# Git branch (optional, defaults to master)
git_branch("main")

# Clone directly without shallow clone
clone_branch_directly(true)

# Keychain name (for CI environments)
# keychain_name("fastlane_keychain")

# Read-only mode (set to true on CI to avoid modifying profiles)
# readonly(true)
