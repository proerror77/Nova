[package]
name = "grpc-tls"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.12"
tokio = { version = "1", features = ["full"] }
anyhow = "1.0"
tracing = "0.1"
rcgen = "0.13"  # For generating self-signed certs in dev/test
rustls = "0.23"
tokio-rustls = "0.26"
pem = "3.0"
x509-parser = "0.16"
chrono = "0.4"

[dev-dependencies]
tokio-test = "0.4"
