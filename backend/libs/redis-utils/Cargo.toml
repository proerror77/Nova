[package]
name = "redis-utils"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
once_cell = "1.19"
redis = { workspace = true }
tokio = { version = "1.36", features = ["rt", "rt-multi-thread", "time", "sync", "macros"] }
tracing = { workspace = true }
