[package]
name = "resilience"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.35", features = ["full"] }
anyhow = "1.0"
tower = { version = "0.4", features = ["limit", "timeout", "buffer"] }
