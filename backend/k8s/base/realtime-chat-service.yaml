---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: realtime-chat-service
  namespace: nova-backend
  labels:
    app: realtime-chat-service
    component: backend
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: realtime-chat-service
  template:
    metadata:
      labels:
        app: realtime-chat-service
        version: v1
    spec:
      containers:
      - name: realtime-chat-service
        image: nova-realtime-chat-service:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8086
          protocol: TCP
        - name: grpc
          containerPort: 9086
          protocol: TCP
        env:
        - name: HTTP_PORT
          value: "8086"
        - name: SERVER_HOST
          value: "0.0.0.0"
        - name: SERVER_PORT
          value: "8086"
        - name: GRPC_PORT
          value: "9086"
        - name: RUST_LOG
          value: "info"
        envFrom:
        - configMapRef:
            name: nova-backend-config
        - secretRef:
            name: nova-backend-secrets
        - secretRef:
            name: nova-matrix-service-token
            optional: true
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/v1/health/live
            port: 8086
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/v1/health/ready
            port: 8086
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          successThreshold: 1
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /api/v1/health
            port: 8086
          initialDelaySeconds: 0
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: realtime-chat-service
  namespace: nova-backend
  labels:
    app: realtime-chat-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8086
    targetPort: 8086
    protocol: TCP
  - name: grpc
    port: 9086
    targetPort: 9086
    protocol: TCP
  selector:
    app: realtime-chat-service
