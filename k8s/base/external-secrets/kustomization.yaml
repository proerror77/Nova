apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nova-staging

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - secretstore.yaml

commonLabels:
  app.kubernetes.io/name: external-secrets
  app.kubernetes.io/part-of: nova-backend
