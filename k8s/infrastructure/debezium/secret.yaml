##############################################################################
# Debezium Secrets
# Contains database credentials for CDC connector
#
# NOTE: Replace placeholder values before deploying!
# In production, use ExternalSecrets or sealed-secrets
##############################################################################

apiVersion: v1
kind: Secret
metadata:
  name: debezium-secrets
  namespace: nova
  labels:
    app: kafka-connect
    component: cdc
type: Opaque
stringData:
  # PostgreSQL password - REPLACE WITH ACTUAL VALUE
  postgres-password: "REPLACE_WITH_POSTGRES_PASSWORD"
