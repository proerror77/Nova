apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nova-staging

# 测试：不引用 base，只引用 microservices
resources:
- ../../../microservices

patchesJson6902:
- target:
    group: apps
    version: v1
    kind: Deployment
    name: analytics-service
    namespace: nova-staging
  patch: |-
    - op: replace
      path: /spec/template/spec/containers/0/imagePullPolicy
      value: Always
