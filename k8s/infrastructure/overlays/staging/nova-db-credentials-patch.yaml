apiVersion: v1
kind: Secret
metadata:
  name: nova-db-credentials
type: Opaque
stringData:
  auth-db-url: postgres://nova:${DB_PASSWORD}@postgres:5432/nova_auth
  graph-db-url: postgres://nova:${DB_PASSWORD}@postgres:5432/nova_graph
