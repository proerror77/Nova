apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: nova
spec:
  template:
    spec:
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: postgres-pvc
