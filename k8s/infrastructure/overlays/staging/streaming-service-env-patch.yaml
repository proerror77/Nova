apiVersion: apps/v1
kind: Deployment
metadata:
  name: streaming-service
  namespace: nova
spec:
  template:
    spec:
      containers:
      - name: streaming-service
        env:
        - name: PORT
          value: "8080"

