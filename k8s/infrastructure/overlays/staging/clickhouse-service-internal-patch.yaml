apiVersion: v1
kind: Service
metadata:
  name: clickhouse-nova-ch
  namespace: clickhouse
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-internal: "true"
spec:
  # Limit exposure to VPC CIDR
  loadBalancerSourceRanges:
    - 10.0.0.0/8

