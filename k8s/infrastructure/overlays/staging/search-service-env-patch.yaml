apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
spec:
  template:
    spec:
      containers:
      - name: search-service
        env:
        - name: PORT
          value: "8080"
        - name: ELASTICSEARCH_URL
          value: "http://elasticsearch.nova-staging.svc.cluster.local:9200"

