apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-gp3
  namespace: nova
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: gp3

