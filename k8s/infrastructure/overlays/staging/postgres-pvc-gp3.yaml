apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-gp3
  namespace: nova-staging
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: gp3

