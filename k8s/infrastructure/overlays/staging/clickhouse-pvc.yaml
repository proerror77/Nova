apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nova-ch-data
  namespace: nova-staging
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 50Gi
