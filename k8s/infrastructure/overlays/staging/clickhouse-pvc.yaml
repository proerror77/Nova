apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nova-ch-data
  namespace: nova-staging
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: standard-rwo
  resources:
    requests:
      storage: 50Gi
