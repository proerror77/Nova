apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
spec:
  template:
    spec:
      containers:
        - name: analytics-service
          env:
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: nova-config
                  key: KAFKA_BROKERS
