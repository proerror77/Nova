apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: nova
spec:
  selector:
    matchLabels:
      component: redis
  template:
    metadata:
      labels:
        component: redis
