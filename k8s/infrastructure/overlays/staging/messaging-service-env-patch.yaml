apiVersion: apps/v1
kind: Deployment
metadata:
  name: messaging-service
spec:
  template:
    spec:
      containers:
      - name: messaging-service
        env:
        - name: MESSAGE_ENCRYPTION_MASTER_KEY
          valueFrom:
            secretKeyRef:
              name: nova-messaging-keys
              key: MESSAGE_ENCRYPTION_MASTER_KEY

