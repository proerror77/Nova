apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
spec:
  template:
    spec:
      containers:
      - name: notification-service
        env:
        - name: PORT
          value: "8080"
        - name: APP_ENV
          value: "staging"

