apiVersion: v1
kind: Service
metadata:
  name: realtime-chat-service
  namespace: nova
spec:
  type: ClusterIP
  selector:
    app: realtime-chat-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 9095
      targetPort: 9095
