apiVersion: v1
kind: Service
metadata:
  name: admin-api
  labels:
    app: admin-api
spec:
  type: ClusterIP
  selector:
    app: admin-api
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
