apiVersion: v1
kind: ServiceAccount
metadata:
  name: alice-voice-service
  labels:
    app: alice-voice-service
  annotations:
    # For GKE Workload Identity (if using GCP Secret Manager)
    # iam.gke.io/gcp-service-account: alice-voice-service@PROJECT_ID.iam.gserviceaccount.com
