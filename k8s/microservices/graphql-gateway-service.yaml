apiVersion: v1
kind: Service
metadata:
  name: graphql-gateway
  namespace: nova
spec:
  type: ClusterIP
  selector:
    app: graphql-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 4000
      targetPort: 4000
