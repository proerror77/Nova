apiVersion: v1
kind: Service
metadata:
  name: search-service
  # NOTE: Namespace is managed by Kustomize overlay - do not set here
spec:
  type: ClusterIP
  selector:
    app: search-service
  ports:
    - name: http
      port: 8086
      targetPort: 8086
      protocol: TCP
