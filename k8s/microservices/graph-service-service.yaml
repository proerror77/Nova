apiVersion: v1
kind: Service
metadata:
  name: graph-service
spec:
  type: ClusterIP
  selector:
    app: graph-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 9091
      targetPort: 9091
