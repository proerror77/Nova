apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-service-config
data:
  LOG_LEVEL: "info"
  ENVIRONMENT: "staging"
  # Kafka configuration
  KAFKA_BROKER: "kafka:9092"
  KAFKA_ENABLED: "true"
