apiVersion: v1
kind: Service
metadata:
  name: admin-web
  labels:
    app: admin-web
spec:
  type: ClusterIP
  selector:
    app: admin-web
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
