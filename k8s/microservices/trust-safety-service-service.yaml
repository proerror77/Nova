apiVersion: v1
kind: Service
metadata:
  name: trust-safety-service
spec:
  type: ClusterIP
  selector:
    app: trust-safety-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 50056
      targetPort: 50056
