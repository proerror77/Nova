apiVersion: v1
kind: Service
metadata:
  name: trust-safety-service
spec:
  type: ClusterIP
  selector:
    app: trust-safety-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 9097
      targetPort: 9097
