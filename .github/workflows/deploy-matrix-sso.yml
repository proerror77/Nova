name: Deploy Matrix SSO (Zitadel + Synapse OIDC)

# 部署 Matrix SSO 組件到 GKE staging 環境
# 組件: Zitadel IdP, Synapse OIDC 配置, 相關 Secrets

on:
  workflow_dispatch:
    inputs:
      deploy_zitadel:
        description: 'Deploy Zitadel IdP'
        type: boolean
        default: true
      deploy_synapse_oidc:
        description: 'Deploy Synapse OIDC config'
        type: boolean
        default: true
      create_secrets:
        description: 'Create/Update secrets (requires GCP Secret Manager)'
        type: boolean
        default: false
      environment:
        description: 'Target environment'
        type: choice
        options:
          - staging
        default: staging

concurrency:
  group: matrix-sso-deploy-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

env:
  GCP_REGION: ${{ vars.GCP_REGION || 'asia-northeast1' }}
  GCP_PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
  GKE_CLUSTER: ${{ vars.GKE_CLUSTER || 'nova-staging-gke' }}
  NAMESPACE: nova-backend

jobs:
  # ============================================================
  # Phase 1: Setup & Validation
  # ============================================================
  setup:
    name: Setup & Validate
    runs-on: ubuntu-latest
    outputs:
      namespace: ${{ steps.config.outputs.namespace }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set configuration
        id: config
        run: |
          if [ "${{ inputs.environment }}" == "staging" ]; then
            echo "namespace=nova-backend" >> "$GITHUB_OUTPUT"
          else
            echo "namespace=nova-backend" >> "$GITHUB_OUTPUT"
          fi

      - name: Validate manifests
        run: |
          echo "🔍 Validating Kustomize manifests..."

          # Check if kustomize can build the overlay
          if [ -f "backend/k8s/overlays/staging/kustomization.yaml" ]; then
            kubectl kustomize backend/k8s/overlays/staging > /dev/null
            echo "✅ Staging overlay is valid"
          else
            echo "❌ Staging overlay not found"
            exit 1
          fi

  # ============================================================
  # Phase 1.5: Initialize Matrix SSO Databases
  # ============================================================
  init-databases:
    name: Initialize Matrix SSO Databases
    needs: setup
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ vars.GCP_PROJECT_ID }}

      - name: Install gke-gcloud-auth-plugin
        run: |
          gcloud components install gke-gcloud-auth-plugin --quiet 2>/dev/null || \
          sudo apt-get install -y google-cloud-sdk-gke-gcloud-auth-plugin

      - name: Get GKE credentials
        run: |
          gcloud container clusters get-credentials ${{ env.GKE_CLUSTER }} \
            --region ${{ env.GCP_REGION }} \
            --project ${{ vars.GCP_PROJECT_ID }}

      - name: Delete previous job if exists
        run: |
          kubectl delete job matrix-sso-db-init -n nova-staging --ignore-not-found=true
          echo "✅ Previous job cleaned up"

      - name: Run database initialization job
        run: |
          echo "🗄️ Initializing Matrix SSO databases..."

          # Apply the database init job
          kubectl apply -f k8s/infrastructure/overlays/staging/matrix-sso-db-init-job.yaml

          # Wait for job to complete
          echo "⏳ Waiting for database initialization..."
          kubectl wait --for=condition=complete job/matrix-sso-db-init -n nova-staging --timeout=120s || {
            echo "❌ Database initialization failed"
            kubectl logs job/matrix-sso-db-init -n nova-staging
            exit 1
          }

          echo "✅ Matrix SSO databases initialized"
          kubectl logs job/matrix-sso-db-init -n nova-staging

  # ============================================================
  # Phase 2: Create Secrets (Optional)
  # ============================================================
  create-secrets:
    name: Create Secrets
    needs: [setup, init-databases]
    # Always run when deploying Zitadel (secrets are required for database connection)
    if: always() && (inputs.create_secrets || inputs.deploy_zitadel) && needs.setup.result == 'success' && needs.init-databases.result == 'success'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ vars.GCP_PROJECT_ID }}

      - name: Install gke-gcloud-auth-plugin
        run: |
          gcloud components install gke-gcloud-auth-plugin --quiet 2>/dev/null || \
          sudo apt-get install -y google-cloud-sdk-gke-gcloud-auth-plugin

      - name: Get GKE credentials
        run: |
          gcloud container clusters get-credentials ${{ env.GKE_CLUSTER }} \
            --region ${{ env.GCP_REGION }} \
            --project ${{ vars.GCP_PROJECT_ID }}

      - name: Ensure namespace exists
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "🏗️ Ensuring namespace exists..."
          kubectl create namespace $NAMESPACE --dry-run=client -o yaml | kubectl apply -f -
          echo "✅ Namespace ready"

      - name: Create Zitadel Secrets
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "🔐 Creating Zitadel secrets..."

          # Generate secure values
          MASTERKEY=$(openssl rand -base64 32 | head -c 32)

          # Use fixed password matching the PostgreSQL user created in postgres-multi-db-init.yaml
          DB_PASSWORD="zitadel_staging_pwd"

          # Get postgres superuser password from nova-db-credentials in nova-staging namespace
          # This is required for Zitadel to run initial migrations
          ADMIN_PASSWORD_DB=$(kubectl get secret nova-db-credentials -n nova-staging -o jsonpath='{.data.DB_PASSWORD}' 2>/dev/null | base64 -d) || ADMIN_PASSWORD_DB="postgres"

          # First user password for Zitadel UI
          ADMIN_PASSWORD="NovaAdmin2024!"

          echo "📋 Using DB_PASSWORD for zitadel user: zitadel_staging_pwd"
          echo "📋 Using ADMIN_PASSWORD from nova-db-credentials secret"

          # Delete existing secret if password mismatch (force update)
          if kubectl get secret zitadel-secrets -n $NAMESPACE &>/dev/null; then
            echo "⚠️  Recreating zitadel-secrets with correct database password..."
            kubectl delete secret zitadel-secrets -n $NAMESPACE
          fi

          kubectl create secret generic zitadel-secrets \
            --namespace=$NAMESPACE \
            --from-literal=ZITADEL_MASTERKEY="$MASTERKEY" \
            --from-literal=ZITADEL_DATABASE_POSTGRES_USER_PASSWORD="$DB_PASSWORD" \
            --from-literal=ZITADEL_DATABASE_POSTGRES_ADMIN_PASSWORD="$ADMIN_PASSWORD_DB" \
            --from-literal=ZITADEL_FIRSTINSTANCE_ORG_HUMAN_PASSWORD="$ADMIN_PASSWORD"
          echo "✅ Created zitadel-secrets with correct database passwords"

      - name: Create Synapse OIDC Secrets
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "🔐 Creating Synapse OIDC secrets..."

          OIDC_SECRET=$(openssl rand -base64 32)
          REGISTRATION_SECRET=$(openssl rand -base64 32)

          if kubectl get secret synapse-oidc-secrets -n $NAMESPACE &>/dev/null; then
            echo "⚠️  Secret synapse-oidc-secrets already exists, skipping..."
          else
            kubectl create secret generic synapse-oidc-secrets \
              --namespace=$NAMESPACE \
              --from-literal=oidc_client_secret="$OIDC_SECRET" \
              --from-literal=registration_shared_secret="$REGISTRATION_SECRET"
            echo "✅ Created synapse-oidc-secrets"
          fi

      - name: Create Identity Service API Key
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "🔐 Creating identity-service API key..."

          API_KEY=$(openssl rand -base64 32)

          if kubectl get secret identity-service-api-key -n $NAMESPACE &>/dev/null; then
            echo "⚠️  Secret identity-service-api-key already exists, skipping..."
          else
            kubectl create secret generic identity-service-api-key \
              --namespace=$NAMESPACE \
              --from-literal=INTERNAL_API_KEY="$API_KEY"
            echo "✅ Created identity-service-api-key"
          fi

  # ============================================================
  # Phase 3: Deploy Zitadel
  # ============================================================
  deploy-zitadel:
    name: Deploy Zitadel
    needs: [setup, init-databases, create-secrets]
    if: always() && inputs.deploy_zitadel && needs.setup.result == 'success' && needs.init-databases.result == 'success'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ vars.GCP_PROJECT_ID }}

      - name: Install gke-gcloud-auth-plugin
        run: |
          gcloud components install gke-gcloud-auth-plugin --quiet 2>/dev/null || \
          sudo apt-get install -y google-cloud-sdk-gke-gcloud-auth-plugin

      - name: Get GKE credentials
        run: |
          gcloud container clusters get-credentials ${{ env.GKE_CLUSTER }} \
            --region ${{ env.GCP_REGION }} \
            --project ${{ vars.GCP_PROJECT_ID }}

      - name: Deploy Zitadel via Kustomize
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "🚀 Deploying Zitadel via Kustomize..."

          # Ensure namespace exists
          kubectl create namespace $NAMESPACE --dry-run=client -o yaml | kubectl apply -f -

          # Check if existing deployment has mismatched selector labels
          # If so, delete it to allow recreation with correct labels
          EXISTING_LABELS=$(kubectl get deployment zitadel -n $NAMESPACE -o jsonpath='{.spec.selector.matchLabels}' 2>/dev/null || echo "{}")
          if echo "$EXISTING_LABELS" | grep -q "app.kubernetes.io/component"; then
            echo "✅ Deployment has correct labels"
          elif echo "$EXISTING_LABELS" | grep -q "zitadel"; then
            echo "⚠️  Deployment has old labels, need to recreate..."
            kubectl delete deployment zitadel -n $NAMESPACE --ignore-not-found=true
            sleep 5
            echo "✅ Old deployment deleted"
          fi

          # Clean up old Zitadel resources for fresh deployment
          echo "🧹 Cleaning up old Zitadel resources..."

          # Delete old deployment to free resources
          kubectl delete deployment zitadel -n $NAMESPACE --ignore-not-found=true
          sleep 2

          # Check PVC status
          PVC_STATUS=$(kubectl get pvc zitadel-data -n $NAMESPACE -o jsonpath='{.status.phase}' 2>/dev/null || echo "NotFound")
          echo "PVC status: $PVC_STATUS"

          # Delete PVC if it's causing node affinity issues or if we need a fresh start
          if [ "$PVC_STATUS" != "NotFound" ]; then
            echo "⚠️  Deleting PVC to resolve node affinity issues..."
            kubectl delete pvc zitadel-data -n $NAMESPACE --ignore-not-found=true
            sleep 3
            echo "✅ PVC deleted, will recreate on available node"
          fi

          # Apply Zitadel resources using server-side apply to handle conflicts
          echo "📦 Applying Zitadel resources..."
          kubectl kustomize backend/k8s/overlays/staging | \
            kubectl apply -f - -n $NAMESPACE --server-side=true --force-conflicts 2>&1 | \
            grep -E "zitadel|Zitadel" | grep -v "ServiceMonitor" || true

          echo "✅ Zitadel resources applied via Kustomize"

          # Verify PVC is bound
          echo "🔍 Verifying PVC..."
          sleep 5
          kubectl get pvc zitadel-data -n $NAMESPACE || true

      - name: Verify and Fix ConfigMap values
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "🔍 Verifying Zitadel ConfigMaps..."

          # Check zitadel-config (environment variables)
          echo ""
          echo "📋 Checking zitadel-config (env vars)..."
          HOST=$(kubectl get configmap zitadel-config -n $NAMESPACE -o jsonpath='{.data.ZITADEL_DATABASE_POSTGRES_HOST}' 2>/dev/null || echo "NOT_FOUND")
          echo "ZITADEL_DATABASE_POSTGRES_HOST = $HOST"

          if [ "$HOST" = "postgres" ]; then
            echo "⚠️  ConfigMap has wrong host, patching directly..."
            kubectl patch configmap zitadel-config -n $NAMESPACE --type='json' \
              -p='[{"op": "replace", "path": "/data/ZITADEL_DATABASE_POSTGRES_HOST", "value": "postgres.nova-staging.svc.cluster.local"}]'
            echo "✅ zitadel-config patched"
          elif [ "$HOST" = "postgres.nova-staging.svc.cluster.local" ]; then
            echo "✅ zitadel-config has correct host"
          else
            echo "❌ Unexpected host value: $HOST"
          fi

          # Check zitadel-runtime-config (yaml config file)
          echo ""
          echo "📋 Checking zitadel-runtime-config (yaml file)..."

          # Apply the staging runtime config patch directly to ensure correct values
          kubectl apply -f backend/k8s/overlays/staging/zitadel-config-patch.yaml -n $NAMESPACE
          echo "✅ zitadel-runtime-config applied"

          # Verify the host in runtime config
          RUNTIME_HOST=$(kubectl get configmap zitadel-runtime-config -n $NAMESPACE -o jsonpath='{.data.zitadel\.yaml}' 2>/dev/null | grep -oP 'Host: \K[^\n]+' | head -1 || echo "NOT_FOUND")
          echo "Runtime config Host = $RUNTIME_HOST"

          echo ""
          echo "📊 All ConfigMap values:"
          kubectl get configmap zitadel-config -n $NAMESPACE -o yaml | grep -E "POSTGRES_HOST|POSTGRES_PORT|POSTGRES_DATABASE" || true

      - name: Apply Zitadel Ingress
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "🚀 Applying Zitadel Ingress..."
          kubectl apply -f backend/k8s/overlays/staging/zitadel-ingress.yaml -n $NAMESPACE
          echo "✅ Zitadel Ingress applied"

      - name: Restart Zitadel deployment
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "🔄 Restarting Zitadel to pick up new configuration..."

          # Delete existing pod to force recreation with new config
          kubectl delete pod -l app=zitadel -n $NAMESPACE --force --grace-period=0 || true

          # Wait a moment for the pod to be deleted
          sleep 5

          # Restart deployment
          kubectl rollout restart deployment/zitadel -n $NAMESPACE

          echo "✅ Zitadel restart initiated"

      - name: Wait for Zitadel deployment
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "⏳ Waiting for Zitadel to be ready..."

          kubectl rollout status deployment/zitadel -n $NAMESPACE --timeout=300s || {
            echo "❌ Zitadel deployment failed"
            echo ""
            echo "📋 Deployment status:"
            kubectl describe deployment/zitadel -n $NAMESPACE

            echo ""
            echo "📋 Pod status:"
            kubectl get pods -l app=zitadel -n $NAMESPACE -o wide

            echo ""
            echo "📋 Init container logs (init-db):"
            for pod in $(kubectl get pods -l app=zitadel -n $NAMESPACE -o jsonpath='{.items[*].metadata.name}'); do
              echo "--- Pod: $pod ---"
              kubectl logs $pod -n $NAMESPACE -c init-db --tail=50 2>/dev/null || echo "No init-db logs"
            done

            echo ""
            echo "📋 Main container logs:"
            for pod in $(kubectl get pods -l app=zitadel -n $NAMESPACE -o jsonpath='{.items[*].metadata.name}'); do
              echo "--- Pod: $pod ---"
              kubectl logs $pod -n $NAMESPACE -c zitadel --tail=50 2>/dev/null || echo "No zitadel logs"
            done

            echo ""
            echo "📋 Pod events:"
            for pod in $(kubectl get pods -l app=zitadel -n $NAMESPACE -o jsonpath='{.items[*].metadata.name}'); do
              echo "--- Pod: $pod ---"
              kubectl describe pod $pod -n $NAMESPACE | tail -20
            done

            exit 1
          }

          echo "✅ Zitadel is ready"
          kubectl get pods -l app=zitadel -n $NAMESPACE

  # ============================================================
  # Phase 4: Deploy Synapse OIDC Config
  # ============================================================
  deploy-synapse-oidc:
    name: Deploy Synapse OIDC Config
    needs: [setup, deploy-zitadel]
    if: always() && inputs.deploy_synapse_oidc && needs.setup.result == 'success'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ vars.GCP_PROJECT_ID }}

      - name: Install gke-gcloud-auth-plugin
        run: |
          gcloud components install gke-gcloud-auth-plugin --quiet 2>/dev/null || \
          sudo apt-get install -y google-cloud-sdk-gke-gcloud-auth-plugin

      - name: Get GKE credentials
        run: |
          gcloud container clusters get-credentials ${{ env.GKE_CLUSTER }} \
            --region ${{ env.GCP_REGION }} \
            --project ${{ vars.GCP_PROJECT_ID }}

      - name: Deploy Matrix well-known config
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "🚀 Deploying Matrix well-known config..."

          kubectl apply -f backend/k8s/overlays/staging/matrix-well-known.yaml -n $NAMESPACE

          echo "✅ Matrix well-known config applied"

      - name: Deploy Synapse OIDC config
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "🚀 Deploying Synapse OIDC configuration..."

          # Apply Synapse config patch and ingress
          kubectl apply -f backend/k8s/overlays/staging/synapse-config-patch.yaml -n $NAMESPACE
          kubectl apply -f backend/k8s/overlays/staging/synapse-ingress.yaml -n $NAMESPACE

          echo "✅ Synapse OIDC config applied"

      - name: Restart Synapse to pick up new config
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "🔄 Restarting Synapse..."

          if kubectl get deployment matrix-synapse -n $NAMESPACE &>/dev/null; then
            kubectl rollout restart deployment/matrix-synapse -n $NAMESPACE
            kubectl rollout status deployment/matrix-synapse -n $NAMESPACE --timeout=300s || {
              echo "⚠️  Synapse restart timeout, checking status..."
              kubectl describe deployment/matrix-synapse -n $NAMESPACE
            }
          else
            echo "⚠️  matrix-synapse deployment not found, skipping restart"
          fi

  # ============================================================
  # Phase 5: Verification
  # ============================================================
  verify:
    name: Verify Deployment
    needs: [setup, deploy-zitadel, deploy-synapse-oidc]
    if: always() && needs.setup.result == 'success'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ vars.GCP_PROJECT_ID }}

      - name: Install gke-gcloud-auth-plugin
        run: |
          gcloud components install gke-gcloud-auth-plugin --quiet 2>/dev/null || \
          sudo apt-get install -y google-cloud-sdk-gke-gcloud-auth-plugin

      - name: Get GKE credentials
        run: |
          gcloud container clusters get-credentials ${{ env.GKE_CLUSTER }} \
            --region ${{ env.GCP_REGION }} \
            --project ${{ vars.GCP_PROJECT_ID }}

      - name: Check deployment status
        env:
          NAMESPACE: ${{ needs.setup.outputs.namespace }}
        run: |
          echo "════════════════════════════════════════════════════"
          echo "📊 Matrix SSO Deployment Summary"
          echo "════════════════════════════════════════════════════"

          echo ""
          echo "🔍 Checking Zitadel..."
          kubectl get deployment zitadel -n $NAMESPACE -o wide 2>/dev/null || echo "Not deployed"
          kubectl get pods -l app=zitadel -n $NAMESPACE 2>/dev/null || true

          echo ""
          echo "🔍 Checking Matrix Synapse..."
          kubectl get deployment matrix-synapse -n $NAMESPACE -o wide 2>/dev/null || echo "Not deployed"
          kubectl get pods -l app=matrix-synapse -n $NAMESPACE 2>/dev/null || true

          echo ""
          echo "🔍 Checking Secrets..."
          kubectl get secret zitadel-secrets -n $NAMESPACE &>/dev/null && echo "✅ zitadel-secrets" || echo "❌ zitadel-secrets missing"
          kubectl get secret synapse-oidc-secrets -n $NAMESPACE &>/dev/null && echo "✅ synapse-oidc-secrets" || echo "❌ synapse-oidc-secrets missing"
          kubectl get secret identity-service-api-key -n $NAMESPACE &>/dev/null && echo "✅ identity-service-api-key" || echo "❌ identity-service-api-key missing"

          echo ""
          echo "🔍 Checking Ingress..."
          kubectl get ingress -n $NAMESPACE 2>/dev/null | grep -E "zitadel|matrix" || echo "No SSO ingress found"

          echo ""
          echo "════════════════════════════════════════════════════"

      - name: Test OIDC Discovery (if Zitadel is exposed)
        continue-on-error: true
        run: |
          echo "🧪 Testing OIDC Discovery endpoint..."

          # This will only work if Zitadel is externally accessible
          curl -s --max-time 10 https://id.staging.nova.app/.well-known/openid-configuration | head -20 || \
            echo "⚠️  OIDC discovery not accessible (may need DNS/ingress setup)"

      - name: Deployment complete
        run: |
          echo ""
          echo "════════════════════════════════════════════════════"
          echo "✅ Matrix SSO Deployment Complete"
          echo "════════════════════════════════════════════════════"
          echo ""
          echo "Next steps:"
          echo "1. Configure DNS for id.staging.nova.app → Zitadel Ingress"
          echo "2. Configure DNS for matrix.staging.nova.app → Synapse Ingress"
          echo "3. Access Zitadel console and configure OIDC Application"
          echo "4. Follow: backend/docs/ZITADEL_UI_SETUP_GUIDE.md"
          echo ""
