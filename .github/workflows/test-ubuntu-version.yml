name: Test Ubuntu Version

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test-ubuntu-version.yml'

jobs:
  test-ubuntu-24:
    name: Test Ubuntu 24.04
    runs-on: ubuntu-24.04

    steps:
      - name: Echo test
        run: echo "✅ Ubuntu 24.04 runner allocated and executing steps!"

  test-ubuntu-22:
    name: Test Ubuntu 22.04
    runs-on: ubuntu-22.04

    steps:
      - name: Echo test
        run: echo "✅ Ubuntu 22.04 runner allocated and executing steps!"
