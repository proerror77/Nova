# Production Environment Configuration

# API Base URL (user-service behind gateway)
VITE_API_BASE=https://api.nova.social

# WebSocket Base URL (messaging-service behind gateway)
VITE_WS_BASE=wss://api.nova.social
