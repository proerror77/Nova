<!--
  Nova Social Info.plist 深层链接配置
  将以下内容添加到你的 Info.plist 文件中
-->

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- 应用名称 -->
    <key>CFBundleDisplayName</key>
    <string>Nova Social</string>

    <!-- Bundle Identifier -->
    <key>CFBundleIdentifier</key>
    <string>com.nova.social</string>

    <!-- 自定义 URL Scheme 配置 -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <!-- URL 类型角色 -->
            <key>CFBundleTypeRole</key>
            <string>Editor</string>

            <!-- URL 名称（通常使用 Bundle ID） -->
            <key>CFBundleURLName</key>
            <string>com.nova.social</string>

            <!-- 自定义 Scheme 列表 -->
            <key>CFBundleURLSchemes</key>
            <array>
                <string>novassocial</string>
            </array>
        </dict>
    </array>

    <!-- 可访问性描述（可选但推荐） -->
    <key>NSCameraUsageDescription</key>
    <string>Nova Social 需要访问相机以拍摄和上传照片</string>

    <key>NSPhotoLibraryUsageDescription</key>
    <string>Nova Social 需要访问相册以选择和上传照片</string>

    <key>NSPhotoLibraryAddUsageDescription</key>
    <string>Nova Social 需要保存照片到相册</string>

    <!-- 推送通知（如果使用） -->
    <key>UIBackgroundModes</key>
    <array>
        <string>remote-notification</string>
    </array>

    <!-- 支持的界面方向 -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>

    <!-- iPad 支持的界面方向 -->
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- 状态栏样式 -->
    <key>UIStatusBarStyle</key>
    <string>UIStatusBarStyleDefault</string>

    <!-- 场景配置（SwiftUI） -->
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
        <key>UISceneConfigurations</key>
        <dict>
            <key>UIWindowSceneSessionRoleApplication</key>
            <array>
                <dict>
                    <key>UISceneConfigurationName</key>
                    <string>Default Configuration</string>
                    <key>UISceneDelegateClassName</key>
                    <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
                </dict>
            </array>
        </dict>
    </dict>

    <!-- 启动屏幕 -->
    <key>UILaunchScreen</key>
    <dict/>

    <!-- 应用传输安全（允许本地开发） -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <!-- 生产环境应移除此项 -->
        <key>NSAllowsLocalNetworking</key>
        <true/>

        <!-- 异常域名（仅开发环境） -->
        <key>NSExceptionDomains</key>
        <dict>
            <key>localhost</key>
            <dict>
                <key>NSExceptionAllowsInsecureHTTPLoads</key>
                <true/>
            </dict>
        </dict>
    </dict>
</dict>
</plist>
